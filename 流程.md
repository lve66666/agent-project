# Pine Agent 当前运行流程、数据路径与设计决策

本文对应当前仓库的实现，包括 CLI、Tkinter GUI、上下文裁剪、JSONL trace，以及 GUI 的只读 Plan Mode。它的目标是解释清楚两件事：模型在什么时候只是在“建议”，本地代码又在什么时候真正决定是否执行。

## 1. 一句话架构

```text
模型：选择下一步建议，返回文本或结构化 tool call
本地代码：验证输入、限制权限、执行工具、保存历史、终止循环、记录 trace
用户：选择工作区、确认命令、审阅/编辑计划、验收代码和测试
```

Pine 不是 LangChain、AutoGen、CrewAI 或 OpenAI Agents SDK 的界面封装。项目自行实现了模型响应解析、工具 schema 与本地执行、对话历史、上下文裁剪、循环终止、错误分类、trace 和 Plan Mode 的阶段切换。

## 2. 目录和关键模块

```text
src/pine/
  cli.py              CLI 入口，组装一次直接执行任务
  gui.py              GUI 入口，提供 Plan Task、审阅与执行界面
  config.py           读取 API 连接环境变量并校验预算
  model_client.py     最小 OpenAI-compatible /chat/completions 客户端
  protocol.py         ToolCall、ToolResult、RunResult、StopReason 数据结构
  agent_loop.py       本地多轮 Agent 状态机
  tool_registry.py    工具 schema、参数校验、工具分派、只读工具集
  workspace.py        文件系统工作区边界、读写和路径保护
  search.py           文本/正则搜索实现
  command_runner.py   命令确认、超时、输出上限和结构化结果
  context.py          请求前的上下文预算和压缩
  planning.py         只读探索后的计划生成
  session.py          按 workspace 隔离的有限任务摘要记忆
  trace.py            append-only JSONL 审计轨迹和敏感信息脱敏
```

## 3. 两条入口路径

### 3.1 CLI：直接执行模式

命令示例：

```powershell
Set-Location G:\编程智能体
$env:PYTHONPATH = "src"
python -m pine.cli "读取 calculator.py，修复除零错误并运行测试" `
  --workspace demo_project `
  --max-turns 10 `
  --max-seconds 180 `
  --trace-dir runs
```

CLI 中 `--yes` 会自动批准模型提出的每条命令。没有 `--yes` 时，终端会在 `run_command` 前询问：

```text
Allow command in <workspace>? <command>
[y/N]
```

CLI 不经过 Plan Mode。它创建完整工具注册表后，立即开始 AgentLoop。

### 3.2 GUI：直接执行或 Plan Mode

启动：

```powershell
Set-Location G:\编程智能体
$env:PYTHONPATH = "src"
python -m pine.gui
```

GUI 有两个入口：

```text
Run Task
  直接进入完整权限的 AgentLoop。

Plan Task
  先运行只读探索 AgentLoop；用户审阅或编辑计划；批准后才运行完整权限 AgentLoop。
```

GUI 的连接信息在 `Connection Settings` 弹窗中输入。API key 仅保存于当前 GUI Python 进程的内存；执行时临时放入环境变量供 `load_settings()` 读取，任务结束后恢复原环境值，不写入源文件或 trace。

## 4. 配置和模型地址

文件：[src/pine/config.py](src/pine/config.py)

```text
OPENAI_API_KEY   必填，HTTP Authorization: Bearer <key>
OPENAI_BASE_URL  可选，默认 https://api.openai.com/v1
OPENAI_MODEL     可选，默认 gpt-4.1-mini
```

客户端在 [src/pine/model_client.py](src/pine/model_client.py) 拼接请求地址：

```python
endpoint = f"{base_url.rstrip('/')}/chat/completions"
```

因此：

```text
OPENAI_BASE_URL=https://api.example.com/v1
最终请求地址=https://api.example.com/v1/chat/completions
```

变量名仍然叫 `OPENAI_*`，并不意味着只能用 OpenAI。只要服务商提供兼容的 Chat Completions 与 tool calling 协议，就可以使用对应的 Base URL、API key 和模型名。

API key 不写在 Python 源码、README、Git 提交、视频字幕或任务文本中。原因是 Git 历史和公开视频很难彻底撤回；泄漏的 key 可能被他人调用并产生费用。`trace.py` 会脱敏字段名中包含 `api_key`、`token`、`password`、`secret` 的值，并替换类似 `sk-...`、`rk-...` 的字符串；这是一层额外保护，不是把 key 写入 trace 的理由。

## 5. 模型 API 协议：模型不能直接碰本地文件

每次模型请求的 JSON 结构大致为：

```json
{
  "model": "模型名",
  "messages": ["system/user/assistant/tool 消息"],
  "tools": ["本轮允许的函数 schema"]
}
```

模型返回两类内容：

```text
普通文本：表示这一轮没有更多工具请求。
tool_calls：表示“建议本地调用某个已注册工具，并给出 JSON 参数”。
```

例如：

```json
{
  "id": "call_read_1",
  "type": "function",
  "function": {
    "name": "read_file",
    "arguments": "{\"path\":\"app.js\"}"
  }
}
```

`parse_chat_completion()` 将其转换为：

```python
ToolCall(
    id="call_read_1",
    name="read_file",
    arguments='{"path":"app.js"}',
)
```

`ToolCall.id` 是协议关联键。模型下一轮收到工具结果时，通过 `tool_call_id="call_read_1"` 知道“这一段文本是我刚才哪一次调用的结果”。同一轮可以有多个工具调用，ID 可以避免结果配错调用。

模型只生成请求；它没有 Python 对象引用、磁盘权限或 shell 权限。真正打开文件、写入文件或运行命令的代码都在本地。

## 6. ToolRegistry：模型建议如何变为本地受限执行

文件：[src/pine/tool_registry.py](src/pine/tool_registry.py)

一个 `ToolDefinition` 包含：

```text
name           工具名
description    给模型看的用途说明
schema         OpenAI-compatible JSON Schema
required       必填字段
argument_types 本地严格类型检查
handler        真正执行的本地函数
```

`ToolRegistry.execute(call)` 的流程：

```text
ToolCall
-> 查找工具名
-> json.loads(arguments)
-> 必须是 JSON object
-> 拒绝未知字段
-> 检查必填字段
-> 检查精确 Python 类型
-> 调用本地 handler
-> 封装 ToolResult(ok/content)
```

模型输出不是可信指令。比如模型请求不存在的 `erase_disk`，注册表不会执行任何函数，而是返回：

```text
unknown tool: erase_disk
```

这个错误文本会作为 `tool` 消息回给模型；模型可以据此改用允许的工具，但本地程序不把“模型说要做”当成授权。

## 7. 完整执行模式的五个工具

`build_default_registry()` 注册：

| 工具 | 参数概念 | 本地行为 |
| --- | --- | --- |
| `list_files(path, depth)` | 目录和深度 | 列出工作区内文件 |
| `search_text(query, path, max_results, use_regex)` | 字符串或正则 | 搜索 UTF-8 源码文本 |
| `read_file(path, start_line, end_line)` | 文件与行范围 | 返回带行号的 UTF-8 文本 |
| `write_file(path, content)` | 文件与文本 | 原子写入工作区文件 |
| `run_command(command, cwd, timeout_seconds)` | shell 命令 | 经用户确认后运行并返回结果 |

`search_text` 的 `use_regex=false` 时会对查询文本使用 `re.escape()`，因此搜索 `a.b` 只匹配字面量 `a.b`。`use_regex=true` 时会把 `a.b` 作为正则，能匹配 `acb`、`a-b` 等任意单字符位置；非法正则会返回结构化错误。搜索会跳过 `.git`、`node_modules`、虚拟环境、二进制、无效 UTF-8 和大于 64 KB 的文件，并把匹配数量限制在 1 到 200 之间。

## 8. Workspace：文件路径边界不是提示词，而是代码检查

文件：[src/pine/workspace.py](src/pine/workspace.py)

所有工具路径先经过：

```python
candidate = (self.root / raw_path).resolve(strict=False)
candidate.relative_to(self.root)
```

含义是先规范化 `..`、绝对路径和符号链接解析结果，再确认目标仍在用户选择的 `workspace.root` 内。

因此以下路径会被拒绝：

```text
../secret.txt    解析后逃出了 workspace
.git/config      明确禁止访问 .git
```

其他限制：

```text
单文件读写最大 64 KiB
只读 UTF-8 文本
二进制文件（包含 NUL 字节）拒绝读取
list_files 跳过 .git/.venv/venv/__pycache__/node_modules
list_files 深度限制为 0..8，最多 500 条记录
```

`write_file` 不直接覆盖目标。它会先在目标父目录创建 `.pine-*` 临时文件，写入、flush、fsync 后再使用 `replace()` 原子替换。若中间失败，临时文件被删除，避免只写了一半的目标文件。

## 9. CommandRunner：命令仍然是风险边界

文件：[src/pine/command_runner.py](src/pine/command_runner.py)

`run_command` 执行前会：

```text
验证 command 非空
验证 timeout_seconds 在 1..120 内
验证 cwd 是 workspace 内目录
询问用户，或由 --yes / Auto-approve commands 统一批准
```

执行使用 `subprocess.run(..., shell=True, capture_output=True, check=False)`；标准输出与错误输出会合并，最多保留 12,000 字符。结果统一为：

```text
approved       用户是否批准
returncode     进程返回码；0 常表示成功
timed_out      是否超过该命令的超时
truncated      输出是否被截断
output         stdout + stderr 的文本
```

重要边界：`cwd` 被限制在 workspace，但 `shell=True` 不是操作系统级沙箱。用户批准的命令仍可能包含绝对路径、网络操作或其他影响系统的 shell 行为。因此“弹窗确认”是本项目的重要安全步骤；演示中可用 Auto-approve 省时，真实使用时不应默认开启。

## 10. AgentLoop：完整执行的本地状态机

文件：[src/pine/agent_loop.py](src/pine/agent_loop.py)

`AgentLoop.run(task)` 创建本次运行的内存历史：

```python
messages = [
    {"role": "system", "content": system_prompt},
    {"role": "user", "content": task},
]
```

之后执行有边界的循环：

```text
for turn in 1..max_turns
  1. 若 cancelled Event 已设置：返回 cancelled
  2. 若当前时间超过 deadline：返回 timeout
  3. ContextWindow.prepare(messages)：得到本轮发送给模型的视图
  4. ModelClient.complete(messages, tool schemas)：请求模型
  5. 将 assistant 回复追加到完整内存 history
  6. 没有 tool_calls：返回 completed + 最终文本
  7. 有 tool_calls：按顺序逐个 ToolRegistry.execute(call)
  8. 将每项结果追加为 role=tool 消息，进入下一轮
```

顺序执行而非并行执行，是为了让同一轮中“先写文件，再读文件”具有确定顺序，避免竞态；项目优先可解释与可复现，而不是并发性能。

工具结果回填格式：

```python
messages.append({
    "role": "tool",
    "tool_call_id": call.id,
    "name": call.name,
    "content": result.content,
})
```

下一轮模型因此能看见文件内容、写入结果、测试输出或错误文本，并可以调整下一步。例如测试失败后，模型可以再次 `read_file` 或 `write_file`，而不是只能一次性猜完答案。

## 11. StopReason：为什么停止由本地代码决定

定义在 [src/pine/protocol.py](src/pine/protocol.py)：

| 原因 | 本地触发条件 | 返回含义 |
| --- | --- | --- |
| `completed` | 模型返回最终文本且没有工具调用 | 正常结束，不自动等于代码一定正确 |
| `max_turns` | 工具循环用完最大模型轮数 | 防止无限循环和费用失控 |
| `timeout` | 整个 AgentLoop 超过总秒数 | 防止长时间占用 |
| `cancelled` | GUI Stop 或外部 Event | 用户主动取消 |
| `model_error` | HTTP 401/403/5xx、网络、客户端请求失败 | 模型服务不可用或配置错误 |
| `protocol_error` | 响应缺少 choices、tool_calls 格式错误等 | 服务响应不符合约定 |

模型不能自行决定“我可以无限继续”或“我已经完成就不需要测试”。`max_turns`、`max_seconds`、工具可见性、参数验证和命令确认都由本地代码掌控。

`RunResult` 保存 `reason`、`summary`、`turns` 和全部 `tool_results`。这让 GUI、CLI、测试和后续审计都可以判断实际用了多少轮、调用了哪些工具、每一项执行是否成功，而不只依赖模型的自然语言总结。

## 12. ContextWindow：运行时上下文怎样保存和裁剪

文件：[src/pine/context.py](src/pine/context.py)

### 12.1 保存位置

对话历史 `messages: list[Message]` 是 `AgentLoop.run()` 的局部 Python 内存变量：

```text
system
user
assistant + tool_calls
tool result
assistant + tool_calls
tool result
...
```

它不是完整会话数据库。Python 进程结束后，完整 `messages` 历史就消失；但 GUI 另有 `SessionStore`，只把任务、最终摘要、停止原因和时间保存成有限摘要。一次 Plan Mode 和批准后的执行模式会各自创建新的 AgentLoop，所以它们各自有独立内存历史；批准后的执行任务会收到“原始任务 + 用户批准计划”，以及可选的最近项目摘要，但不会自动带入所有原始只读工具输出。

### 12.4 持久化摘要记忆

文件：[src/pine/session.py](src/pine/session.py)

GUI 默认打开 `Use project memory`。`SessionStore` 根据 workspace 的规范化绝对路径计算 SHA-256 前缀，把记录保存到：

```text
G:\编程智能体\sessions\<workspace-hash>.jsonl
```

不同 workspace 使用不同文件，避免把 web_demo 的历史带到 grade_demo。默认最多保留 20 条，每条任务和摘要最多 2,000 字符，注入模型的 memory 总量最多 8,000 字符。写入使用临时文件、flush、fsync 和原子 replace；文件损坏的单行会被跳过。记录前会复用 trace 的敏感信息脱敏逻辑，并且从不保存完整工具调用、源码、API key 或命令输出。

新任务的消息顺序是：

```text
system prompt
user 当前任务
system Project memory（最近摘要，可由 Use project memory 关闭）
```

memory 是参考资料，不是事实保证；system prompt 仍要求模型检查当前文件。GUI 的 `Chat History` 显示这些摘要，`Clear History` 只删除当前 workspace 对应的摘要文件。关闭记忆只停止本次注入，不会自动删除已有摘要。

### 12.2 请求视图与原始历史分离

每轮请求前：

```python
request_messages = self.context.prepare(messages)
```

```text
messages          完整内存历史，不直接修改
request_messages  本轮发送给模型的受预算副本
```

这保证本地仍保有本次运行的完整事实，而模型请求不会无限增长。

### 12.3 两层压缩策略

默认配置：

```python
ContextWindow(max_chars=24000, max_tool_chars=8000)
```

第一层：单个 `tool` 输出超过 8,000 字符时，复制消息并保留头部和尾部，中间加入：

```text
... tool output truncated: <原长度> -> 8000 chars ...
```

保留开头是因为文件导入、函数定义常在前面；保留结尾是因为测试报错、堆栈和退出状态常在末尾。只修改即将请求模型的副本，不修改原始 `messages`、`ToolCall.id`、工具名或 trace。

第二层：总消息 JSON 字符数超过 24,000 时：

```text
保留最初 system 消息
保留最初 user 任务
按 assistant 开始位置划分完整 turn group
从最新 group 往前保留，直到预算用完
插入 “Earlier history was compacted locally ...” 摘要
```

必须按 group 裁剪，不能任意删除单条消息。否则可能留下 assistant 的 `tool_call_id`，却删除对应 `role=tool` 结果，破坏模型工具调用协议。

当前预算是 JSON 字符数近似值，不是针对某个模型的精确 token 计数。优点是无需额外 tokenizer、适用于不同 OpenAI-compatible 服务；缺点是中文、英文和代码的字符/token 比例不同，因此它是保守的工程限制。

## 13. GUI Plan Mode：批准前只读探索

文件：[src/pine/planning.py](src/pine/planning.py) 与 [src/pine/gui.py](src/pine/gui.py)

点击 GUI 的 `Plan Task` 后，并不是一次无工具聊天，而是运行一个有相同终止预算的“只读 AgentLoop”：

```text
用户任务
-> build_read_only_registry(workspace)
-> AgentLoop(system_prompt=PLANNING_SYSTEM_PROMPT)
-> 模型可多轮 list_files/search_text/read_file
-> tool result 回填下一轮
-> 模型最终文本成为计划
-> Review Plan 可编辑窗口
```

只读注册表 `build_read_only_registry()` 只向模型公开：

```text
list_files
search_text
read_file
```

其中没有：

```text
write_file
run_command
```

所以规划模型可以先看到真实项目结构、函数定义和已有测试，再提出有针对性的计划；但批准前不能修改文件，也不能运行 shell 命令。即使模型违规伪造 `write_file`，注册表没有该工具，结果只会是 `unknown tool: write_file`，不会创建文件。

规划阶段与执行阶段都受 `max_turns` 和 `max_seconds` 限制。它们是两个独立循环，因此最多可能各用一份预算：例如 GUI 填 10 轮，最坏情况是规划最多 10 轮，批准后执行又最多 10 轮。这是“先理解、后执行”带来的明确成本。

审阅窗口有三种结果：

```text
编辑计划：直接在文本框修改计划
Reject Plan：记录 plan_rejected，不启动完整工具循环
Execute Approved Plan：记录 plan_approved，启动完整工具循环
```

批准后实际执行任务文本包含：

```text
原始用户任务
+ 用户已批准/编辑的计划
+ “计划只是指导，仍须检查实际文件、保持边界、运行合适验证”
```

计划不是新的权限来源。批准不会绕过 `Workspace`、`ToolRegistry`、命令确认、超时或最大轮数；它只是把用户审阅后的实现方向提供给完整执行 AgentLoop。

## 14. GUI 线程和可见事件

Tkinter 主线程只负责界面渲染、计划审阅和命令确认弹窗。模型请求和 AgentLoop 在后台线程运行；事件通过 `queue.Queue` 回传主线程。

执行区中：

```text
[Plan 1] ...  表示只读探索阶段
[Turn 1] ...  表示批准后的完整执行阶段，或直接 Run Task 阶段
```

可见事件来自本地 AgentLoop：

```text
model_request
assistant_reply
tool_result
model_error / protocol_error
run_finished
```

GUI 的 Stop 设置 `threading.Event`。正在进行的 HTTP 请求不能被强制中断，但请求返回后，循环在下一次检查时会返回 `cancelled`。

## 15. Trace：保存到磁盘的是什么，没保存什么

文件：[src/pine/trace.py](src/pine/trace.py)

运行时创建：

```text
<trace-dir>/<uuid>.jsonl
```

默认 CLI 相对路径是 `runs`。如果当前目录是 `G:\编程智能体`，文件通常在：

```text
G:\编程智能体\runs\<随机运行ID>.jsonl
```

JSONL 是“一行一个 JSON 事件”，便于追加写入、查看和视频演示。直接执行通常含有：

```text
run_started
model_request
assistant_reply
tool_result
run_finished
```

Plan Mode 的同一个 trace 还会记录：

```text
run_started (mode=plan)
plan_requested
... 只读模型/工具事件 ...
plan_created
plan_approved 或 plan_rejected
execution_started（仅批准后）
... 完整执行事件 ...
```

trace 是审计线索，不是完整可恢复会话：`model_request` 当前只保存轮数和 message_count，不保存本轮完整 messages。工具结果会被记录，因此 trace 仍可能包含源码、测试输出或任务文本；`runs/` 已在 `.gitignore` 中，不应把它当作公开、安全的长期存储。摘要记忆位于单独的 `sessions/`，同样已加入 `.gitignore`，且只保存有限脱敏摘要。

## 16. 如何判断 Agent 没有“随便说完成”

不要只看最终自然语言。验收证据应至少包括：

```text
1. GUI/trace：确实发生 read/search/write/run 的多轮工具调用。
2. git diff：检查 Agent 改了哪些文件，特别是没有通过修改/删除测试作弊。
3. 独立测试：任务结束后由用户手动再次运行测试。
4. 可见行为：网页任务要在浏览器验证；CLI 任务要检查实际输出或文件内容。
```

例如 `web_demo` 的修复完成后：

```powershell
Set-Location G:\编程智能体\web_demo
node --test test_app.js
```

应看到全部测试通过；然后在浏览器中输入空成绩，确认页面显示友好提示而不是 `NaN`。再检查：

```powershell
Set-Location G:\编程智能体
git diff -- web_demo\app.js
git diff -- web_demo\test_app.js
```

理想情况只应修改生产逻辑 `app.js`，而不是把失败测试删掉或降低测试要求。

## 17. Git 的位置

Git 不是模型工具，也不在 Plan Mode 的只读工具中。项目 Agent 不会自动 `commit` 或 `push`，因为提交和推送会改变公开历史，应该由用户在审查 diff 后决定。

典型流程：

```powershell
git status
git diff
python -m unittest discover -s tests -v
git add -- <明确的文件>
git commit -m "feat: 描述本次修改"
git push
```

不要使用 `git add .` 盲目提交。它可能把 API 配置、trace、视频、临时文件或不属于本次任务的 demo 改动一起带入提交。

## 18. 测试策略和当前证据

单元测试在 `tests/`，使用 FakeModel 预设模型回复，不依赖真实网络、API key 或模型随机性。重点覆盖：

```text
多轮 tool call -> tool result -> 下一轮请求
未知工具的拒绝
停止原因和预算
路径逃逸、.git、二进制、大文件保护
命令确认、超时、输出截断
search_text 的字面量/正则行为
上下文压缩不破坏 tool_call_id 配对
只读 Plan Mode 能读取文件，不能写文件或运行命令
```

截至本文更新，运行：

```powershell
$env:PYTHONPATH = "src"
python -m unittest discover -s tests -v
powershell -ExecutionPolicy Bypass -File tools/verify.ps1
```

得到 27 个测试通过；Windows 未启用符号链接权限时，符号链接逃逸测试会被跳过。

## 19. 当前能力和诚实边界

当前适合：

```text
小到中型代码修复
多文件阅读、搜索与修改
测试失败后的再修复
网页/脚本 demo
先只读探索再由用户批准执行
用 trace 和 Git diff 复盘过程
```

当前不等于：

```text
操作系统级沙箱
持久化的跨任务项目记忆
精确 token 计费器
自动恢复未完成任务的工作流引擎
无人工确认即可安全执行任意 shell 的系统
```

特别是 `run_command` 使用 shell，Workspace 只能保证文件工具的路径边界，不能把用户批准的命令限制成无副作用。因此正确的使用方式是：Plan Mode 先理解任务，用户审阅方案，执行阶段仍审阅命令、测试、diff 和实际行为。

## 20. 答辩可直接使用的说明

> Pine 将“模型建议”和“本地执行权限”分开。模型只通过 OpenAI-compatible tool calling 返回结构化请求；本地 ToolRegistry 验证工具名、JSON 参数、字段和类型，Workspace 检查路径边界，CommandRunner 进行确认、超时和输出限制。AgentLoop 在本地维护多轮消息历史，把工具结果以 tool 消息回填给下一轮，并由本地 max_turns、max_seconds、取消信号和错误分类决定何时结束。
>
> GUI 的 Plan Mode 不是单纯让模型写计划。它先使用只读工具 list_files、search_text、read_file 探索真实代码，因此能基于实际文件提出方案；但 write_file 和 run_command 根本不在规划阶段的工具 schema 中。用户可以编辑、拒绝或批准计划。批准后才创建完整工具 AgentLoop，而且批准不绕过命令确认和工作区边界。
>
> 上下文在每个 AgentLoop 的内存消息列表中保存。请求模型前，ContextWindow 对大工具输出做头尾截断，再按完整 assistant/tool 回合保留最近历史，避免超出上下文且不丢失 tool_call_id 配对。磁盘上的 JSONL trace 是审计记录，不是完整会话记忆。最终是否正确不依赖模型的“已完成”文本，而依赖测试、Git diff、实际运行结果和 trace 共同验证。
