# Pine Agent 运行流程与设计决策

## 一、整体流程

评委真正想考察的是：是否知道模型做了什么、本地程序做了什么，以及为什么要这样划分职责。

```text
用户输入任务
-> CLI 解析参数
-> 读取模型配置
-> 创建 workspace 和工具
-> 把任务与工具 schema 发送给模型
-> 模型返回文本或工具调用
-> 本地验证并执行工具
-> 把工具结果放回对话历史
-> 再请求模型
-> 达到完成、超时或轮数上限时停止
```

## 二、程序启动：cli.py

文件：`src/pine/cli.py`

用户执行：

```powershell
python -m pine.cli `
  "修复 calculator.py 的除零错误" `
  --workspace demo_project `
  --max-turns 10 `
  --max-seconds 180 `
  --trace-dir runs `
  --yes
```

### 1. 解析参数

`build_parser()` 定义 `task`、`workspace`、`max-turns`、`max-seconds`、`yes` 和 `trace-dir`。解析后分别保存在 `args.task`、`args.workspace` 等字段中。

设计原因：把任务、工作目录、预算和确认策略作为显式参数，避免重要行为隐藏在代码或配置中。

### 2. 读取配置

`cli.py:35` 调用 `load_settings()`，配置文件为 `src/pine/config.py`。

读取的环境变量：

```text
OPENAI_API_KEY
OPENAI_BASE_URL
OPENAI_MODEL
```

这里的 `OPENAI_*` 只是变量名。只要服务商提供 OpenAI 兼容接口，也可以指向其他服务。

API key 不写入源代码、Git、README、视频或运行轨迹。没有 key 时，程序返回退出码 `2`，不进入 Agent 循环。

### 3. 创建 workspace

`args.workspace.resolve()` 把工作目录转换为绝对路径，然后创建 `Workspace`。

`Workspace` 负责文件路径检查、文件读取、文件写入、目录遍历、文件大小限制和二进制检测。

设计原因：Agent 不应该拥有整个磁盘的访问权，只应该访问用户指定的项目目录。

### 4. 创建命令执行器

`CommandRunner` 负责执行测试命令、设置工作目录、超时、捕获 stdout/stderr、截断过长输出和用户确认。

模型生成的命令只是建议，不是授权。默认需要用户确认，`--yes` 只在明确允许自动执行时使用。

### 5. 创建工具注册表

`build_default_registry()` 注册当前五个工具：

```text
list_files
search_text
read_file
write_file
run_command
```

设计原因：工具越少，权限越小，行为越容易测试和解释。

### 6. 创建模型客户端

`OpenAICompatibleClient` 只负责构造 HTTP 请求、发送 JSON、接收模型响应和解析响应，不负责读文件、写文件、执行 shell 或决定循环。

职责划分：

```text
ModelClient = 通信层
AgentLoop = 控制层
ToolRegistry = 工具层
Workspace/CommandRunner = 执行层
```

### 7. 创建轨迹记录器

`TraceWriter` 创建 `runs/随机ID.jsonl`，记录任务开始、模型请求、模型回复、工具结果和任务结束。

设计原因：Agent 不应该只是最后说一句“完成”，还应该能复查经历了哪些步骤。

## 三、模型请求：model_client.py

第一次请求模型时，发送的内容大致是：

```json
{
  "model": "模型名",
  "messages": [
    {"role": "system", "content": "系统提示"},
    {"role": "user", "content": "用户任务"}
  ],
  "tools": [
    "list_files",
    "search_text",
    "read_file",
    "write_file",
    "run_command"
  ]
}
```

`src/pine/model_client.py:28` 会把基础地址拼接为：

```python
self.endpoint = f"{base_url.rstrip('/')}/chat/completions"
```

例如：

```text
base_url = https://example.com/v1
最终地址 = https://example.com/v1/chat/completions
```

模型服务端只负责分析任务和生成 JSON，不直接接触本地文件。

## 四、模型如何决定调用工具

模型看到工具 schema 后，判断下一步需要什么。例如返回：

```json
{
  "id": "call-1",
  "type": "function",
  "function": {
    "name": "read_file",
    "arguments": "{\"path\":\"calculator.py\"}"
  }
}
```

`parse_chat_completion()` 将它转换为：

```python
ToolCall(
    id="call-1",
    name="read_file",
    arguments='{"path":"calculator.py"}'
)
```

模型只是生成“我要调用 read_file 以及参数”，没有真正打开文件。

## 五、工具注册与本地执行

文件：`src/pine/tool_registry.py`

`ToolDefinition` 描述一个工具的名称、描述、schema、必填字段、参数类型和本地 handler。

`ToolRegistry.execute()` 的执行过程：

```text
收到 ToolCall
-> 查找工具名
-> 解析 arguments JSON
-> 检查必填字段
-> 检查未知字段
-> 检查参数类型
-> 调用本地函数
-> 返回 ToolResult
```

模型不能直接调用任意 Python 函数，只能调用注册过的工具。

## 六、文件工具：workspace.py

收到 `read_file` 或 `write_file` 后，程序先执行：

```python
candidate = (self.root / raw_path).resolve(strict=False)
candidate.relative_to(self.root)
```

这会防止：

```text
../secret.txt
.git/config
```

同时拒绝二进制文件、非法 UTF-8 文件和超过 64 KB 的文件。

写入使用临时文件再替换目标文件，避免程序中途失败时留下半截文件。

设计原因：使用最小权限原则，只允许 Agent 操作用户明确指定的 workspace。

## 七、命令工具：command_runner.py

命令通过：

```python
subprocess.run(...)
```

执行时设置：

```text
cwd             命令运行目录
timeout         超时限制
capture_output  捕获标准输出和标准错误
check=False     自己处理返回码
```

结果分为：

```text
returncode == 0  命令成功
returncode != 0  命令失败
timed_out=True   命令超时
approved=False   用户没有批准
```

当前版本不是操作系统级沙箱。文件工具严格限制在 workspace 内，但用户批准的 shell 命令仍可能访问绝对路径。这是需要主动说明的工程边界。

## 八、工具结果如何回到模型

工具执行后生成：

```python
ToolResult(
    tool_call_id="call-1",
    name="read_file",
    ok=True,
    content="文件内容"
)
```

然后追加到消息历史：

```python
messages.append({
    "role": "tool",
    "tool_call_id": call.id,
    "name": call.name,
    "content": result.content,
})
```

下一轮模型看到：

```text
用户任务
-> assistant 请求读取文件
-> tool 返回文件内容
```

模型就能根据刚刚读取的内容继续请求 `write_file` 或 `run_command`。

`ToolCall.id` 是请求和结果的关联键。一轮同时调用多个工具时，例如 `call-1` 读取 a.py、`call-2` 读取 b.py，模型可以准确知道每个结果对应哪次调用。

## 九、AgentLoop 的完整控制逻辑

文件：`src/pine/agent_loop.py`

循环从以下代码开始：

```python
for turn in range(1, self.max_turns + 1):
```

每一轮依次执行：

```text
检查是否取消
-> 检查总时间
-> 准备上下文
-> 请求模型
-> 解析模型回复
-> 有工具调用就执行
-> 没有工具调用就结束
```

如果有工具调用：

```python
result = self.tools.execute(call)
```

如果没有工具调用：

```python
return RunResult(
    StopReason.COMPLETED,
    reply.content or "Task completed.",
    turn,
    tuple(results)
)
```

## 十、停止条件

### `completed`

模型没有继续请求工具，而是返回最终文本。表示本地 Agent 正常结束。

### `max_turns`

模型一直请求工具，达到 `--max-turns` 上限。用于防止无限循环和 API 费用失控。

### `timeout`

整个任务超过 `--max-seconds` 上限。

### `cancelled`

用户或外部程序设置取消信号。

### `model_error`

网络失败、HTTP 401、HTTP 403、HTTP 500 或 API key 错误。

### `protocol_error`

模型返回格式错误，例如没有 `choices`、`tool_calls` 不是列表或工具调用缺少 ID。

最终停止原因定义在：

```text
src/pine/protocol.py
```

## 十一、为什么循环由本地代码控制

### 1. 模型是不可信输入

模型可能重复调用工具、生成错误命令、错误判断任务完成或一直请求文件。不能把最终控制权交给模型。

### 2. 控制成本

每次模型请求都可能消耗时间、配额和费用。`max_turns` 和 `max_seconds` 可以限制资源消耗。

### 3. 控制权限

模型只能建议调用工具，本地程序仍然检查路径、参数、用户确认、超时和输出长度。

### 4. 保证可测试

本地程序能够稳定测试：

```text
没有工具调用 -> completed
轮数耗尽 -> max_turns
时间耗尽 -> timeout
网络错误 -> model_error
格式错误 -> protocol_error
```

核心原则：

```text
模型负责决定“下一步建议”
本地 Agent 负责决定“是否允许、如何执行、何时停止”
```

## 十二、上下文管理：context.py

对话历史会不断增加：

```text
system
user
assistant tool call
tool result
assistant tool call
tool result
...
```

历史过长时，`ContextWindow.prepare()` 保留：

```text
system 消息
用户原始任务
最近完整的工具调用和工具结果组
```

不能只删除任意几条消息，否则可能留下 assistant 的工具调用，却删除对应的 tool 结果，破坏模型协议。

当前使用字符数作为上下文预算近似值；更完善的版本可以使用 tokenizer 计算精确 token 数量。

## 十三、运行轨迹：trace.py

`TraceWriter` 使用 JSONL 记录：

```text
run_started
model_request
assistant_reply
tool_result
run_finished
```

选择 JSONL 的原因：依赖少、可以追加写入、容易查看、适合演示和故障复盘。

轨迹会对以下内容脱敏：

```text
api_key
token
password
sk-...
```

## 十四、为什么使用 FakeModel 测试

FakeModel 预先返回：

```text
write_file
-> read_file
-> 最终回答
```

这样测试不依赖网络、API key、模型随机性或服务商状态，可以稳定验证：

```text
工具是否被调用
消息是否正确回填
循环是否停止
错误是否分类
轮数是否统计正确
```

真实 API 只需要少量集成测试。

## 十五、为什么不自动 Git commit/push

Git 提交和推送由用户控制，因为用户需要审查修改，避免错误代码、API key 或未经批准的文件进入公开仓库，也避免破坏题目要求的提交历史。

## 十六、当前实现的诚实边界

当前版本可以完成：

```text
小型和中型编程任务
多文件读写
代码搜索
运行测试
根据测试失败继续修复
运行轨迹记录
```

但它不是：

```text
操作系统级安全沙箱
大型项目自动开发系统
完全可靠的自主编程系统
多 Agent 协作平台
```

已知限制：

1. `run_command` 使用 `shell=True`；
2. 单文件读取上限为 64 KB；
3. 上下文使用字符数估算；
4. 依赖模型是否支持 tool calling；
5. 没有复杂的自动恢复机制；
6. 终端最终摘要较简洁；
7. 真实效果受模型能力和 API 稳定性影响。

## 十七、面试回答模板

> 用户首先通过 CLI 输入任务和 workspace。`cli.py` 读取环境变量中的模型配置，创建受限 Workspace、CommandRunner、ToolRegistry、ModelClient 和 TraceWriter，最后启动我自己实现的 AgentLoop。
>
> AgentLoop 将系统提示、用户任务和工具 schema 发送给模型。模型不会直接访问文件，而是返回结构化 tool call。`model_client.py` 自己解析模型 JSON，转换成 `ToolCall`。`ToolRegistry` 检查工具名、JSON 参数、必填字段和参数类型，验证通过后调用本地 `Workspace` 或 `CommandRunner`。
>
> 工具执行结果被封装成 `ToolResult`，再以 `role=tool` 消息放回对话历史，下一轮发送给模型。模型可以根据读取结果或测试错误继续请求工具。
>
> 如果模型没有继续返回工具调用，本地循环以 `completed` 结束；如果超过最大轮数，以 `max_turns` 结束；如果超过时间预算，以 `timeout` 结束；网络问题和协议问题分别返回 `model_error` 和 `protocol_error`。
>
> 这些控制逻辑都由本地代码实现，没有使用 LangChain、AutoGen、OpenAI Agents SDK 或其他 Agent 框架。模型只负责建议下一步动作，本地程序负责验证、执行、记录和停止。
